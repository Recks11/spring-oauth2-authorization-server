server:
  port: 8000
spring:
  application:
    name: REX-AUTH
  data:
    mongodb:
      username: idea
      password: ideapass
      database: authserver
      auto-index-creation: true

#logging:
#  level:
#    root: debug
oauth2:
  openid:
    implicit:
      enabled: true
